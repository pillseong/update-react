import React from "react";
import "./Main.css";
import Header from "../header/Header";

// 자유게시판 (free), 로그인 (login), 소개 (Notice)
function Main() {
  return (
    <div>
      <Header />
    </div>
  );
}

export default Main;
