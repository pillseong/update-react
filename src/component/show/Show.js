import React from "react";
import Header from "../header/Header";

// 자유게시판 (free), 로그인 (login), 소개 (Notice)
function Show() {
  return (
    <div>
      <Header />
      <h1>소개</h1>
    </div>
  );
}

export default Show;
